<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.valentina.colombiaturistica">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity_Publicidad">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AcercaActivity" />
        <activity android:name=".HotelesActivity" />
        <activity android:name=".BaresActivity" />
        <activity android:name=".SitiosActivity" />
        <activity android:name=".InfoActivity"></activity>
    </application>

</manifest>
